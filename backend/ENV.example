# Copy to .env and edit values as needed
# Used for local development only

# Bind HTTP (optional; disabled when DISABLE_HTTP=1)
PORT=3000
HOST=0.0.0.0

# Public base URL to embed in responses (set to your LAN HTTPS URL)
PUBLIC_BASE_URL=https://192.168.0.101:3443

# Netlify site URL for bootstrap redirects
FRONTEND_URL=https://temp-files.netlify.app

# HTTPS server (run with npm run start:https)
HTTPS_PORT=3443

# Set to 1 to prevent starting HTTP listener from server.js
DISABLE_HTTP=1


